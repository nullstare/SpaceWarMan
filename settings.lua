return {
	locale = "en",
	window = {
		monitor = 0,
		scale = 5,
		fullscreen = false,
		vsync = true,
	},
	keys = {
		exit = KEY_ESCAPE,
		right = KEY_RIGHT,
		left = KEY_LEFT,
		jump = KEY_X,
		shoot = KEY_C,
	},
}
